sudo docker run -d --name lair -p 80:8080 --link spawning_pool wordpress

**wordpress container: background task
**named lair
**bind port 80 to port 8080
should be able to use DB container spawning-pool as a DB service
access it via the web browser
do this in 2 commands
